[build]
builder = "nixpacks"
rootDirectory = "backend"

[deploy]
startCommand = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"

[[crons]]
schedule = "*/15 * * * *"
command = "python scripts/dispatch_crons.py"
