[build]
builder = "nixpacks"

[deploy]
startCommand = "npx paperclipai@0.3.1 onboard --yes --headless"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[service]
internalPort = 3100
