[build]
builder = "nixpacks"

[deploy]
startCommand = "npx paperclipai@latest onboard --yes --headless"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[service]
internalPort = 3100
